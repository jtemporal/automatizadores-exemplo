dist: trusty

script: python -c "print('Olá, mundo!')"

before_deploy:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker pull python:3-slim

deploy:
  provider: script
  script: bash build.sh
  on:
    branch: master
